# paymentdomain-api-mapper
This page shows most of the possible APIs which are needed for payments domain.  This has clear segregation between Must have and nice to have features.
The page is work in progress for other domains.
